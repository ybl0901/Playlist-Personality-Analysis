# 听见你｜交接说明

通过歌单生成音乐人格画像的单页应用，目标渠道是小红书。结合大五人格（OCEAN）与 MBTI 进行分析。

## 本地启动

需要 Node.js >= 22（项目依赖 vinext，Node 20 不兼容）。

```bash
npm install
npm run dev
```

打开 `http://localhost:3000/`。

正式构建：

```bash
npm run build
```

## 2026-08-25 更新内容

### 第一步：歌单读取修复（已完成）

**问题**：原代码使用旧版接口 `https://music.163.com/api/playlist/detail`，该接口已返回错误码 20001，导致所有网易云歌单读取失败，页面显示"暂时无法读取"。

**修复**：
- 主接口改用 `https://music.163.com/api/v6/playlist/detail?id={id}`，可获取歌单信息和全部 trackIds
- 通过 `https://music.163.com/api/song/detail?ids=[...]` 分批（每批100首）批量获取歌曲详情
- 最多读取前 500 首用于分析（歌单可能有数千首，500首足够准确）
- 保留降级方案：主接口失败时从公开网页提取可见歌曲
- 新增 QQ 音乐歌单读取支持（`c.y.qq.com` 接口）
- 酷狗等暂不支持自动读取的平台，提示用户直接粘贴歌名

**验证结果**：
- 测试链接 `https://music.163.com/playlist?id=806898079` 成功读取
- 歌单名："望川水-喜欢的音乐"，共 2445 首，实际读取 500 首
- 带 uct2 参数的链接同样正常（只取 id 参数）

### 第二步：AI 分析服务（已完成，AI 增强可选）

**新增文件**：
- `lib/analyzer.ts`：音乐分析引擎
  - 200+ 歌手风格映射库
  - 10 种情绪关键词匹配
  - 语言/年代/BPM/场景推断
  - 大五人格（OCEAN）五维评分
  - MBTI 四维度倾向计算
  - 9 种音乐人格模型（嘻哈态度表达者、电子派对动物、热血摇滚青年、深夜民谣诗人、治愈系聆听者、都市夜晚漫步者、古典优雅品鉴者、多元音乐探索家、流行音乐达人）
  - 小红书标题/正文/话题标签自动生成
- `app/api/analyze/route.ts`：分析 API
  - POST 接收歌曲列表，返回完整分析结果
  - 支持可选 AI 增强（见下方环境变量）
  - 未配置 AI Key 时使用规则引擎，功能完整可用

**AI 增强需要的 API Key**：
配置以下环境变量后，会调用 AI 生成个性化的"音乐故事"和"深度解读"：
- `AI_API_KEY`：API 密钥（必填才能启用 AI）
- `AI_BASE_URL`：OpenAI 兼容接口地址（默认 `https://api.openai.com/v1`，豆包用 `https://ark.cn-beijing.volces.com/api/v3`）
- `AI_MODEL`：模型名（默认 `gpt-4o-mini`，豆包可用 `doubao-1-5-pro-32k-250115`）

**未配置 AI Key 时**：规则引擎生成的报告包含人格描述、大五人格、MBTI、风格/情绪分布、高频歌手、小红书文案，功能完整。

### 第三步：免费开放

当前版本暂不收费。完整人格档案、关系模式、穿衣风格和能量补给方式均在生成报告后直接展示。

### 第四步：小红书优化（已完成）

- 结果页响应式设计，适合手机截图
- 自动生成小红书标题（如"我的音乐人格是「嘻哈态度表达者」，准到离谱"）
- 自动生成正文和话题标签（#听见你 #音乐人格 #歌单人格测试 #MBTI 等）
- Hero 区突出"歌单比自我介绍诚实"
- 一键复制小红书文案
- 不包含"女性更愿付费"等未经验证的结论
- 保留"结果用于娱乐与自我探索，不构成心理评估"声明

### 第五步：其他修复

- 创建缺失的 `build/sites-vite-plugin.ts`（原项目从 GPT 导出时缺失，导致 dev/build 失败）
- Node 版本要求 22+（原项目 package.json 已声明，但需注意本机 Node 版本）

### 2026-08-25 二次更新

- **全量读取**：移除 500 首限制，改为 200 首/批 × 5 并发，2445 首歌约 4 秒读取完成
- **心路历程**：新增按歌曲添加顺序的 5 阶段风格演变时间线（最初→现在），每阶段显示主导风格、情绪、代表歌曲、常听歌手，自动识别风格转变节点
- 扩充英文情绪关键词库，提升英文歌情绪识别率
- dev 服务器支持 `--hostname 0.0.0.0`，手机同 WiFi 可通过局域网 IP 访问

## 修改的文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `app/api/playlist/route.ts` | 重写 | v6接口+批量获取全部歌曲+QQ音乐支持 |
| `app/api/analyze/route.ts` | 新增 | AI分析API（规则引擎+可选AI增强） |
| `lib/analyzer.ts` | 新增 | 音乐分析引擎（大五人格+MBTI+9种人格） |
| `app/page.tsx` | 重写 | 接入新API，展示大五人格/MBTI/分布图表/完整免费档案 |
| `app/globals.css` | 修改 | 新增数据概览、雷达图、情绪轨道、时间线、人格形象样式 |
| `build/sites-vite-plugin.ts` | 新增 | 修复缺失的vite插件 |
| `.env.example` | 新增 | 环境变量配置示例 |

## 如何测试

```bash
# 1. 启动
npm run dev

# 2. 浏览器打开 http://localhost:3000/

# 3. 粘贴网易云歌单链接测试
# https://music.163.com/playlist?id=806898079
# 应能读取到"望川水-喜欢的音乐"500首歌并生成报告

# 4. 或点击"填入示例歌单"快速测试

# 5. 命令行测试API
curl "http://localhost:3000/api/playlist?url=https%3A%2F%2Fmusic.163.com%2Fplaylist%3Fid%3D806898079"
curl -X POST http://localhost:3000/api/analyze -H "Content-Type: application/json" -d '{"tracks":[{"name":"晴天","artists":"周杰伦"}]}'

# 6. 构建验证
npm run build
```

## 仍需要账号或密钥的功能

1. **AI 增强分析**：需要 `AI_API_KEY`（OpenAI 或豆包 ARK API Key）。未配置时规则引擎正常工作。
2. 当前版本不接入支付，先用于免费体验和开源验证。
3. **网易云读取**：当前使用公开接口，非官方商业合作。规模化前应确认平台授权或改成用户主动粘贴曲目。

## 已验证示例

- 链接 `https://music.163.com/playlist?id=806898079`：成功读取"望川水-喜欢的音乐"500首，生成"嘻哈态度表达者"人格报告（MBTI: ENFJ）
- 带 uct2 参数的链接同样正常
- `npm run build` 构建成功，4个路由全部通过
